import { EventEmitter } from '@angular/core';
export declare class FileUploaderComponent {
    allowMultiple: boolean;
    fileType: string;
    required: boolean;
    maxSizeInKb: number;
    onSelection: EventEmitter<FileList>;
    DisplayedText: string;
    fileChange(event: any): boolean;
}
